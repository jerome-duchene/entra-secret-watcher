# entra-secret-watcher
Lightweight, self-hosted Docker container that monitors Microsoft Entra ID app registration secrets and certificates for expiration. Sends proactive notifications via Gotify, Email, or Microsoft Teams (Graph API).
